let 
    var n := 10
    var z := 100
in 
    z := for n := 1 to n do (
        z := z - 25;
        while (n < z) do (n := n + 1; if n > 10 then break);
        z := n - 1;
        if z < 50 then break 
    )
end